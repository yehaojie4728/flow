# FlowGap Problem Decomposition

Generated by `docs/prompts/00_codex_bootstrap_analyze_only.md`.

---

## 1. eBPF Event Collection

- **Goal:** Non-intrusively collect AscendCL memcpy/sync/device/memory events from Ascend 910A/910B hosts using eBPF uprobes.
- **Input files:**
  - AscendCL runtime library (libascendcl.so or equivalent);
  - eBPF trace definitions from reference repo (HostDiag);
  - target API list: `aclrtMemcpy`, `aclrtMemcpyAsync`, `aclrtSynchronizeStream`, etc.
- **Output files:**
  - `src/collector/ebpf_hooks/` — eBPF programs for each hook point;
  - `src/collector/ring_buffer_reader` — user-space ring buffer consumer;
  - trace output in FlowEvent binary format;
  - `docs/collector_schema.md` — event format documentation.
- **Evidence needed:**
  - NEED_DATA: actual Ascend trace files;
  - NEED_TEACHER_CONFIRMATION: CANN version, eBPF toolkit version;
  - NEED_EXPERIMENT: eBPF overhead measurement (RQ8).
- **Risk:** HIGH — eBPF hooks may not work on this machine's CANN version; hook points may differ from reference repo.
- **TODO:**
  - [ ] Verify CANN/AscendCL library paths on target machine.
  - [ ] Determine which API surfaces are available for uprobe.
  - [ ] Adapt HostDiag eBPF hooks or write new ones.
  - [ ] Test ring buffer throughput.

---

## 2. Event Normalization

- **Goal:** Resolve raw event addresses to device/path/link identifiers; infer direction, path_id, link_bitmap; estimate async memcpy intervals.
- **Input files:**
  - Raw FlowEvent stream from eBPF collector;
  - topology map (NUMA layout, NPU id, PCIe/HCCS topology);
  - address-to-device mappings from `aclrtMallocHost`, `aclrtMallocDevice`, `aclrtSetDevice`.
- **Output files:**
  - `src/normalizer/event_normalizer.py` — event normalization pipeline;
  - `src/normalizer/address_map.py` — address-to-device mapping;
  - `src/normalizer/topo.py` — topology data structures;
  - normalized event stream with path_id, link_bitmap, quality_flags.
- **Evidence needed:**
  - NEED_DATA: Ascend server topology information;
  - NEED_TEACHER_CONFIRMATION: whether address mappings can be reliably built from eBPF events.
- **Risk:** MEDIUM — address mapping may be incomplete or unreliable for certain allocation patterns.
- **TODO:**
  - [ ] Define path_id enumeration scheme.
  - [ ] Implement address map maintenance from eBPF malloc/free/set_device events.
  - [ ] Implement path resolution from src/dst device and NUMA topology.
  - [ ] Implement quality_flags for unknown paths.

---

## 3. Path/Link Busy-Idle Timeline

- **Goal:** Convert normalized events into per-path and per-link busy-idle interval representations using probe-aware intervalization.
- **Input files:**
  - Normalized event stream with path_id, link_bitmap, timestamps, size, direction.
- **Output files:**
  - `src/predictor/timeline.py` — busy-idle timeline builder;
  - `src/predictor/intervalization.py` — probe-aware interval merging;
  - per-path and per-link busy interval sets with merged micro-gaps.
- **Evidence needed:**
  - NEED_EXPERIMENT: RQ3 representation ablation showing probe-aware intervalization outperforms fixed windows.
- **Risk:** LOW — algorithmic component, no hardware dependency.
- **TODO:**
  - [ ] Implement interval insertion with O(log n) per-path interval trees.
  - [ ] Implement micro-gap merging: merge gaps < T_probe_min + M.
  - [ ] Support incremental online updates.
  - [ ] Unit tests with synthetic event sequences.

---

## 4. Async Interval Estimation

- **Goal:** Produce conservative timeline (for safety) and estimated timeline (for model learning) for async memcpy events.
- **Input files:**
  - Normalized events with async_flag, stream_id;
  - `aclrtSynchronizeStream` events for stream completion.
- **Output files:**
  - `src/predictor/async_estimator.py` — async interval estimation;
  - conservative_timeline and estimated_timeline for each path/link.
- **Evidence needed:**
  - NEED_EXPERIMENT: RQ7.1 async uncertainty experiment.
- **Risk:** MEDIUM — accuracy depends on stream sync event availability; missing sync events degrade estimation.
- **TODO:**
  - [ ] Implement conservative timeline: use stream sync return time as upper bound.
  - [ ] Implement estimated timeline: use bandwidth model + stream ordering.
  - [ ] Implement async margin tracking based on estimation error.
  - [ ] Handle multi-stream concurrency.

---

## 5. Safe-Window Prediction

- **Goal:** Predict multi-horizon survival probability S(h | x_t) = P(T_next > h | x_t) for each path, and produce gap lower-bound estimates.
- **Input files:**
  - Per-path busy-idle timelines;
  - Recent gap/burst statistics;
  - path/link/stream/phase/resource features.
- **Output files:**
  - `src/predictor/survival_predictor.py` — multi-horizon survival model;
  - `src/predictor/features.py` — feature extraction;
  - `src/predictor/lower_bound.py` — conservative gap lower-bound estimation;
  - per-path SafeWindow publications.
- **Model recommendation:** tiered approach — EWMA/quantile baseline (Tier 0) → GBDT survival predictor (Tier 1) → semi-Markov state model (Tier 2).
- **Evidence needed:**
  - NEED_EXPERIMENT: RQ2 safe-window prediction accuracy;
  - NEED_EXPERIMENT: RQ3 representation ablation;
  - NEED_TRACE: real Ascend traces for training/evaluation.
- **Risk:** HIGH — prediction quality entirely depends on trace data availability and quality.
- **TODO:**
  - [ ] Implement phase-aware EWMA/quantile baseline predictor.
  - [ ] Implement multi-horizon GBDT survival predictor.
  - [ ] Implement feature extraction pipeline.
  - [ ] Implement gap lower-bound estimation using quantile regression or conformal prediction.
  - [ ] Train/evaluate on real Ascend traces.
  - [ ] Compare with baselines: fixed-window, raw-event, EWMA-only.

---

## 6. Confidence Calibration

- **Goal:** Calibrate model output probabilities to match actual safe ratios; compose comprehensive confidence score from model, calibration, data quality, and drift components.
- **Input files:**
  - Raw predictor output probabilities;
  - historical prediction outcomes (safe vs. overlapped);
  - data quality metrics (ring buffer drops, unknown paths, async uncertainty);
  - drift statistics.
- **Output files:**
  - `src/predictor/calibrator.py` — empirical calibration bins + drift detection;
  - calibration table indexed by probability bins;
  - C_final = min(C_model, C_calib, C_data, C_drift).
- **Evidence needed:**
  - NEED_EXPERIMENT: calibration error (ECE) before/after calibration (RQ2.2);
  - NEED_EXPERIMENT: RQ7.3 workload drift recovery.
- **Risk:** MEDIUM — calibration needs sufficient historical data; cold-start period may have uncalibrated confidence.
- **TODO:**
  - [ ] Implement empirical calibration bins.
  - [ ] Implement data quality confidence scoring.
  - [ ] Implement drift detector (rolling statistics, distribution divergence).
  - [ ] Unit tests with known probability distributions.

---

## 7. Prober Scheduling

- **Goal:** Decide when and how to probe based on SafeWindow publications; support probe type selection, downgrade, cancellation, and budget enforcement.
- **Input files:**
  - SafeWindow publications from predictor;
  - probe cost model;
  - prober state (pending probes, budget).
- **Output files:**
  - `src/prober/scheduler.py` — probe admission control;
  - `src/prober/prober.py` — probe execution;
  - `src/prober/budget.py` — probe budget tracker.
- **Evidence needed:**
  - NEED_EXPERIMENT: RQ4 scheduling overlap reduction;
  - NEED_EXPERIMENT: RQ5 workload interference.
- **Risk:** LOW — scheduler logic is relatively straightforward; depends on predictor accuracy.
- **TODO:**
  - [ ] Implement probe admission: check G_lower > T_probe + M and C > eta.
  - [ ] Implement probe type selection based on confidence tier.
  - [ ] Implement probe downgrade (bandwidth → latency → tiny → cancel).
  - [ ] Implement probe cancellation on late unsafe prediction.
  - [ ] Implement probe budget enforcement.

---

## 8. Analyzer Feedback

- **Goal:** Analyze probe results for detection utility; feed actual overlap, duration, and prediction error back to predictor for online calibration.
- **Input files:**
  - probe results (path, type, latency, bandwidth, start/end timestamps);
  - actual business memcpy timeline;
  - predicted SafeWindow.
- **Output files:**
  - `src/analyzer/analyzer.py` — probe result analysis;
  - `src/analyzer/feedback.py` — feedback loop to predictor;
  - detection utility metrics.
- **Evidence needed:**
  - NEED_EXPERIMENT: RQ6 detection utility.
- **Risk:** MEDIUM — analyzer scope must stay limited (not full root cause diagnosis).
- **TODO:**
  - [ ] Implement overlap detection between probe and business traffic.
  - [ ] Implement feedback: prediction_error, overlap_flag, probe_duration.
  - [ ] Implement simple anomaly detection for evaluation (injected faults).
  - [ ] Implement failure-aware probing mode (lower confidence threshold, confirmation probes).

---

## 9. SoCC Evaluation and Paper Writing

- **Goal:** Execute experiments (RQ1-RQ8), produce figures, write paper, ensure SoCC compliance.
- **Input files:**
  - All system modules;
  - Real Ascend traces;
  - Synthetic workload generator;
  - Fault injection scenarios.
- **Output files:**
  - `paper/main.tex` and `paper/sections/*.tex`;
  - `paper/figures/*.pdf`;
  - `paper/references.bib`;
  - `results/*.csv` and `results/*.json`;
  - `docs/ai_usage_disclosure.md`;
  - `docs/submission_checklist_socc2026.md`.
- **Evidence needed:**
  - NEED_EXPERIMENT: All RQ1-RQ8 experiments;
  - NEED_CITATION: Related work references (Hostping, HostDiag, eBPF, survival analysis);
  - NEED_FINAL_CHECK: SoCC compliance (format, anonymity, page limit, PDF size).
- **Risk:** HIGH — evaluation depends on trace data, hardware access, and sufficient time.
- **TODO:**
  - [ ] RQ1: Traffic characterization with real Ascend traces.
  - [ ] RQ2: Safe-window prediction accuracy.
  - [ ] RQ3: Representation ablation.
  - [ ] RQ4: Scheduling effectiveness.
  - [ ] RQ5: Workload interference.
  - [ ] RQ6: Detection utility.
  - [ ] RQ7: Robustness (async, event loss, drift).
  - [ ] RQ8: System overhead.
  - [ ] Sensitivity analysis.
  - [ ] Related work survey and BibTeX collection.
  - [ ] Write paper sections.
  - [ ] Create figures.
  - [ ] Anonymity check.
  - [ ] Format compliance check.
  - [ ] AI usage disclosure.
  - [ ] Reserve reviewer preparation.
